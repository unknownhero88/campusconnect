<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name=".testDB.DB_test"
            android:exported="false" />
        <activity
            android:name=".LostANDFound.add_lost_found_items"
            android:exported="false" />
        <activity
            android:name=".Community.addPost_community"
            android:exported="false" />
        <activity
            android:name=".Community.full_post_community"
            android:exported="false" />
        <activity
            android:name=".DigitalNoticeBoard.details_lost_found"
            android:exported="false" />
        <activity
            android:name=".Home.Home"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>